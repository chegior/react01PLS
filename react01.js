/**@jsx React.DOM */

var Hello = React.createClass({ //create a component
    render:function() { ///you have to supply a function
        return <div>
                    <h1>Hello REACT </h1>

                </div>
    }//the return is process at the JSX VS Reac

});